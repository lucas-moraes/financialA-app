import React from 'react';
import { Text, View } from 'react-native';
import { styles } from './styles';

export const Navbar = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>CloudCont</Text>
    </View>
  );
};
