import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconExpand = () => {
  return <Icon name="expand-more" size={25} color={THEME.COLORS.TABLE} />;
};
