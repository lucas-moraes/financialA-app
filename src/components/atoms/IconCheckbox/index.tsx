import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconCheckbox = () => {
  return <Icon name="check-box" size={20} color={THEME.COLORS.CHECK_BOX} />;
};
