import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconSearch = () => {
  return <Icon name="search" size={25} color={THEME.COLORS.TABLE} />;
};
