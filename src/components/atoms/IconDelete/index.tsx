import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconDelete = () => {
  return <Icon name="delete" size={20} color={THEME.COLORS.TABLE} />;
};
