import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconCancelCircle = () => {
  return <Icon name="cancel" size={40} color={THEME.COLORS.CANCEL} />;
};
