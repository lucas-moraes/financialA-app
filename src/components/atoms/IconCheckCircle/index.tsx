import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconCheckCircle = () => {
  return <Icon name="check-circle" size={40} color={THEME.COLORS.CHECK_BOX} />;
};
