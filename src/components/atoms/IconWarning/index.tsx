import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconWarning = () => {
  return <Icon name="warning" size={60} color={THEME.COLORS.WARNING} />;
};
