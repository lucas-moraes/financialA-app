import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { THEME } from '../../../theme';

export const IconEdit = () => {
  return <Icon name="edit" size={20} color={THEME.COLORS.TABLE} />;
};
